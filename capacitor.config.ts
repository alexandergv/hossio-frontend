import type { CapacitorConfig } from '@capacitor/cli';

// to be used for webview
const config: CapacitorConfig = {
  appId: 'hossio.netlify.app',
  appName: 'Hossio',
  webDir: 'hossio.netlify.app'
};

export default config;
